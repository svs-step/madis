<div class="modal fade" id="mesurement-modal" tabindex="-1" role="dialog" aria-labelledby="mesurement-modal-label">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel">{{ 'registry.mesurement.modal.title'|trans }}</h4>
            </div>
            <div class="modal-body">
                <form id="mesurement-modal-form" data-target="#mesurement-modal" action="{{ path('api_registry_mesurement_modal_create') }}" method="POST">
                    <div class="form-group">
                        <label for="mesurement-name" class="control-label">{{ 'registry.mesurement.form.name'|trans }}</label>
                        <input type="text" class="form-control" id="mesurement-name" name="mesurement[name]" maxlength="255" required>
                    </div>
                    <div class="form-group">
                        <label for="mesurement_planificationDate" class="control-label">{{ 'registry.mesurement.form.planification_date'|trans }}</label>
                        <input type="text" id="mesurement_planificationDate" name="mesurement[planificationDate]" class="datepicker form-control">
                    </div>
                    <div class="form-group">
                        <label for="mesurement_manager" class="control-label">{{ 'registry.mesurement.form.manager'|trans }}</label>
                        <input type="text" id="mesurement_manager" name="mesurement[manager]" class="form-control" maxlength="255">
                    </div>
                    <div class="form-group">
                        <label for="mesurement_priority" class="control-label">{{ 'registry.mesurement.form.priority'|trans }}</label>
                        <select id="mesurement_priority" name="mesurement[priority]" class="form-control">
                            <option value=""></option>
                            {% for key, priority in dictionary('registry_mesurement_priority') %}
                                <option value="{{ key }}">{{ priority }}</option>
                            {% endfor %}
                        </select>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'registry.mesurement.modal.cancel'|trans }}</button>
                <button id="mesurement-modal-form-button" form="mesurement-modal-form" type="submit" class="btn btn-primary">{{ 'registry.mesurement.modal.create'|trans }}</button>
            </div>
        </div>
    </div>
</div>

