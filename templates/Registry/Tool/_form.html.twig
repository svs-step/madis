{% if form.vars.value.createdAt.timestamp is defined %}
    {% set submitValue = submitValue|default('registry.tool.action.edit_submit'|trans) %}
{% else %}
    {% set submitValue = submitValue|default('registry.tool.action.new_submit'|trans) %}
{% endif %}

{{ form_start(form) }}

<div class="row">

    <div class="col-md-6">
        {# GENERAL INFORMATIONS #}
        <div class="box box-solid box-info">
            <div class="box-header with-border"><h3 class="box-title">{{ 'registry.tool.tab.general'|trans }}</h3></div>
            <div class="box-body">
                {{ form_row(form.name) }}
                {{ form_row(form.type) }}
                {{ form_row(form.editor) }}
                {{ form_row(form.manager) }}
                {{ form_row(form.description) }}
                {{ form_row(form.contractors) }}
                {{ form_row(form.prod_date) }}
                {{ form_row(form.country_type) }}
                {{ form_row(form.country_name) }}
                {{ form_row(form.country_guarantees) }}
                {{ form_row(form.other_info) }}
            </div>
        </div>
    </div>

    <div class="col-md-6">
        {# RELATIONS #}
        <div class="box box-solid box-primary">
            <div class="box-header with-border"><h3 class="box-title">{{ 'registry.tool.tab.measures'|trans }}</h3></div>
            <div class="box-body">
                {{ form_rest(form) }}
            </div>
        </div>
    </div>

</div>

<div class="row">
    <div class="col-md-12">
        <div class="box box-solid">
            <div class="box-body text-center">
                <a href="{{ path('registry_tool_list') }}" class="btn btn-default">
                    {{ 'registry.tool.action.back_to_list'|trans }}
                </a>
                <input type="submit" class="btn btn-primary" value="{{ submitValue }}">
            </div>
        </div>
    </div>
</div>

{{ form_end(form) }}


