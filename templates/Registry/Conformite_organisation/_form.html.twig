{% set submitValue = submitValue|default('action.submit'|trans) %}

{{ form_start(form) }}

<div class="row">
    <div class="col-md-12">
        <div class="box box-solid">
            <div class="box-body text-center">
                <a href="{{ path('registry_mesurement_list') }}" class="btn btn-default">
                    {{ 'registry.mesurement.action.back_to_list'|trans }}
                </a>
                <input type="submit" class="btn btn-primary" value="{{ submitValue }}">
            </div>
        </div>
    </div>
</div>

<div class="row">

</div>

{{ form_row(form.date) }}
{{ form_row(form.participants) }}
{% include 'Registry/Conformite_organisation/_form_reponse.html.twig' with {'form': form.conformites} %}

{{ form_end(form) }}
