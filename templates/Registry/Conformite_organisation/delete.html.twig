{% extends 'base.html.twig' %}

{% set bodyClass = 'registry_conformite_organisation delete' %}
{% set menuItem = 'registry_conformite_organisation' %}

{% block title %}{{ 'registry.conformite_organisation.title.delete'|trans }} - {{ parent() }}{% endblock %}

{% block body_head %}
    <h1>
        {{ 'registry.conformite_organisation.title.delete'|trans }}
        <small>{{ object }}</small>
    </h1>
{% endblock %}

{% block breadcrumb %}
    {% set breadcrumb = [
        { 'name': 'registry.conformite_organisation.breadcrumb.list'|trans, 'link': path('registry_conformite_organisation_list') },
        { 'name': 'registry.conformite_organisation.breadcrumb.delete'|trans }
    ] %}
    {% include '_breadcrumb.html.twig' with {'breadcrumb': breadcrumb} %}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-xs-12">
            <div class="box box-solid">
                <div class="box-header"></div>
                <div class="box-body">
                    <p>{{ 'registry.conformite_organisation.label.delete_confirm'|trans({ '%object%': object }) }}</p>
                </div>
                <div class="box-footer">
                    <a href="{{ path('registry_conformite_organisation_list') }}" class="btn btn-default">
                        {{ 'global.action.back_to_list'|trans }}
                    </a>
                    <a href="{{ path('registry_conformite_organisation_delete_confirm', { 'id': object.id }) }}" class="btn btn-primary">
                        {{ 'global.action.confirm_delete'|trans }}
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
