{% extends 'base.html.twig' %}

{% set bodyClass = 'registry_collectivity' %}

{% block title %}{{ 'registry.collectivity.title.edit'|trans }} - {{ parent() }}{% endblock %}

{% block body_head %}
    <h1>
        {{ 'registry.collectivity.title.edit'|trans }}
        <small>{{ form.vars.value }}</small>
    </h1>
{% endblock %}

{% block breadcrumb %}
    {% set breadcrumb = [
        { 'name': 'registry.collectivity.breadcrumb.list'|trans, 'link': path('registry_collectivity_list') },
        { 'name': 'registry.collectivity.breadcrumb.edit'|trans({'%name%': form.vars.value}) }
    ] %}
    {% include '_breadcrumb.html.twig' with {'breadcrumb': breadcrumb} %}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-xs-12">
            {{ include('Registry/Collectivity/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
