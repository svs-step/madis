{% extends 'base.html.twig' %}

{% set bodyClass = 'registry_request delete' %}
{% set menuItem = 'registry_request' %}

{% block title %}{{ 'registry.request.title.delete'|trans }} - {{ parent() }}{% endblock %}

{% block body_head %}
    <h1>
        {{ 'registry.request.title.delete'|trans }}
    </h1>
{% endblock %}

{% block breadcrumb %}
    {% set breadcrumb = [
        { 'name': 'registry.request.breadcrumb.list'|trans, 'link': path('registry_request_list') },
        { 'name': 'registry.request.breadcrumb.delete'|trans }
    ] %}
    {% include '_breadcrumb.html.twig' with {'breadcrumb': breadcrumb} %}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-xs-12">
            <div class="box box-solid">
                <div class="box-header"></div>
                <div class="box-body">
                    Souhaitez vous vraiment archiver {{ objects_length }} demande{{objects_length > 1 ? 's' : '' }} ?
                </div>
                <div class="box-footer">
                    <a aria-label="{{ 'registry.mesurement.action.back_to_list'|trans }}" href="{{ path('registry_request_list') }}" class="btn btn-default">
                        {{ 'registry.mesurement.action.back_to_list'|trans }}
                    </a>
                    <a aria-label="{{ 'action.confirm_delete'|trans }}" href="{{ path('registry_request_delete_all_confirm', { 'ids': ids }) }}" class="btn btn-primary">
                        {{ 'action.confirm_delete'|trans }}
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
