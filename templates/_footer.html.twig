<footer id="main-footer" class="main-footer" role="contentinfo" tabindex="-1">

    <strong><i class="fa-regular fa-copyright small"></i> 2018 <a href="{{ app_footer_provider_url }}" target="_blank" rel="nofollow noreferrer noopener">{{ app_footer_provider_name }}</a></strong>
    {% if app.user %}: {{ app.user.collectivity }}{% endif %}

    <div class="pull-right">
        {% if app.user %}
            <a href="{{ path('page_declaration') }}">Accessibilité : {{ 'page.declaration.result'|trans }}</a>
            <span aria-hidden="true" style="padding: 0 5px;">|</span>
            <a href="{{ path('page_sitemap') }}">Plan du site</a>
            <span aria-hidden="true" style="padding: 0 5px;">|</span>
            <a href="{{ path('page_credit') }}">Crédit</a>
            <span aria-hidden="true" style="padding: 0 5px;">|</span>
        {% endif %}
        v{{ app_version }}
    </div>
</footer>
